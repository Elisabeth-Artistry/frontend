const initialState = {
    cart: [],
    processing: false,
    error: ''
}

const reducer = (state = initialState, action) => {
    switch(action.type){
        
    }
    return state
}

export default reducer